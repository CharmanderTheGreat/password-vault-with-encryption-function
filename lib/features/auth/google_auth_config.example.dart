/// Template for google_auth_config.dart, which is gitignored.
///
/// To set this project up: copy this file to google_auth_config.dart
/// (same folder), then fill in your own Desktop OAuth Client ID and
/// Secret from Google Cloud Console > APIs & Services > Credentials.
class GoogleAuthConfig {
  static const desktopClientId = 'PASTE_YOUR_DESKTOP_CLIENT_ID_HERE';
  static const desktopClientSecret = 'PASTE_YOUR_DESKTOP_CLIENT_SECRET_HERE';
}
